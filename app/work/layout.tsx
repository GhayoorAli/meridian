export const metadata = {
  title: "Work",
  description:
    "Selected architecture, landscape, and research works by Meridian Studio.",
};

export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return children;
}
